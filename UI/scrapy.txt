
        <div class="loading" id="loading">
            <div>Analyzing market data...</div>
            <div class="loading-dots">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>


<div class="chat-container" id="chatContainer">
        <div class="chat-header">
            <div>
                <i class="fas fa-robot"></i>
                Smart Buy Assistant
            </div>
            <button class="chat-toggle" onclick="toggleChat()">
                <i class="fas fa-times"></i>
            </button>
        </div>
        <div class="chat-messages" id="chatMessages">
            <div class="welcome-message">
                Ask me anything about e-commerce analytics! 👋
            </div>
        </div>
        <div class="chat-input">
            <input type="text" placeholder="Type your question..." id="chatInput" onkeypress="handleChatKeyPress(event)">
            <button onclick="sendChatMessage()">Send</button>
        </div>
    </div>





===============================================================
.loading{
    display:none;
    text-align:center;
    padding:2rem;
    color:var(--text-light);
}

.loading-dots{
    display:inline-flex;
    gap:.3rem;
    margin-top:1rem;
}

.loading-dots span{
    width:8px;
    height:8px;
    border-radius:50%;
    background:#9ca3af;
    animation:loading 1.4s infinite ease-in-out both;
}

.loading-dots span:nth-child(1){animation-delay:-0.32s;}
.loading-dots span:nth-child(2){animation-delay:-0.16s;}

@keyframes loading{
    0%,80%,100%{transform:scale(0);}
    40%{transform:scale(1);}
}


.chat-container{
    position:fixed;
    bottom:1.8rem;
    right:1.8rem;
    width:min(380px,92vw);
    height:min(500px,75vh);
    background:white;
    border-radius:20px;
    box-shadow:0 20px 40px rgba(0,0,0,0.1);
    display:none;
    flex-direction:column;
    overflow:hidden;
    z-index:1000;
}

.chat-header{
    padding:1.3rem;
    background:var(--accent);
    color:white;
    display:flex;
    justify-content:space-between;
    align-items:center;
}

.chat-toggle{
    background:none;
    border:none;
    color:white;
    font-size:1.4rem;
    cursor:pointer;
}

.chat-messages{
    flex:1;
    padding:1rem;
    overflow-y:auto;
}

.welcome-message{
    text-align:center;
    color:#6b7280;
    padding:.8rem;
}

.chat-input{
    padding:1rem;
    border-top:1px solid var(--border);
    display:flex;
    gap:.5rem;
}

.chat-input input{
    flex:1;
    padding:.7rem 1rem;
    border:1px solid var(--border);
    border-radius:25px;
    outline:none;
}

.chat-input button{
    padding:.7rem 1.2rem;
    background:var(--accent);
    color:white;
    border:none;
    border-radius:25px;
    cursor:pointer;
    transition:.25s;
}

.chat-input button:hover{
    background:var(--accent-light);
}





@media (max-width:768px){

    .header{
        padding:1rem;
    }

    .main-content{
        padding:1.5rem 1rem;
    }

    .features{
        grid-template-columns:1fr;
        gap:1.5rem;
    }

}